/* globe-rgn */
/* ============================================================
   globe-region.js — REMOVED.
   The standalone region tool was superseded by the ◰ button on
   each data overlay: corner-pick or preset (Niño, IOD, ATL3 …)
   region extraction with time-series plot and CSV export.
   The no-op below keeps the call site in globe.js harmless.
   ============================================================ */
window.GlobeRegion = function(){ return {}; };

